#include<iostream>
using namespace std;

int main()
{
    int package;
    cout<<"Enter package: ";
    cin>>package;
    if(package > 10)
    {
        cout<<"Accpeted";
    }
    else
    {
        cout<<"Rejected";
    }
}